defaultNamespace: kured
helm:
  releaseName: kured
  chart: kured
  repo: https://kubereboot.github.io/charts
  values:
    configuration:
      rebootSentinelCommand: \"! needs-restarting --reboothint\"
      useRebootSentinelHostPath: false
  #version: 100.1.2+up19.0.3
  #kustomize:
  #  dir: overlays/dev
    #    valuesInline:
    #      configuration:
    #        rebootSentinelCommand: "! needs-restarting --reboothint"     # command for which a successful run signals need to reboot (default ""). If non-empty, sentinel file will be ignored.
